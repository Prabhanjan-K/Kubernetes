apiVersion: v1
kind: Service
metadata:
  name: {{ include "httpdhelm.fullname" . }}
  labels:
    {{- include "httpdhelm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "httpdhelm.selectorLabels" . | nindent 4 }}
