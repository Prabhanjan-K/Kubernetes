apiVersion: v1
kind: Service
metadata:
  name: {{ include "mynginxhelm.fullname" . }}
  labels:
    {{- include "mynginxhelm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mynginxhelm.selectorLabels" . | nindent 4 }}
