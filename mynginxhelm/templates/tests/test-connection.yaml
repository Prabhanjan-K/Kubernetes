apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mynginxhelm.fullname" . }}-test-connection"
  labels:
    {{- include "mynginxhelm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mynginxhelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
